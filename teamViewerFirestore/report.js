/*
let reportData = new Map;
const getDataFromLocalStorageToMapForReport = () => {
    for (let i = 0; i < localStorage.length; i++) {
        let keys = (localStorage.key(i));
        let get = JSON.parse(localStorage.getItem(keys));
        reportData.set(keys, get);
    }

};
getDataFromLocalStorageToMapForReport();
*/